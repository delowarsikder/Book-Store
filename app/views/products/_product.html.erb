
  <tbody>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.description %></td>
        <td class="col-md-2 px-0 img-fluid"> <%= image_tag(product.image_url)%></td>
        <td ><%= product.price %></td>
        <td><%= link_to "Details", product, class:"btn btn-primary" %></td>
      </tr>
    </tbody>

<%#  %>