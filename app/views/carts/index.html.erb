<p style="color: green"><%= notice %></p>

<div id="carts">
  <% @carts.each do |cart| %>
    <%= render cart%>
    <p>
      <%= link_to "Show this cart", cart %>
    </p>
  <% end %>
</div>

<%= link_to "New cart", store_index_path %>
